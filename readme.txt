Brady Messer
CPSC 4040 Fall 2019
HW5

makefile commands:
  - make
    - Compiles the program and puts the executable 'warper' in the same directory
      as the makefile
  - make run
    - Calls make then runs the program with the image waves.png


All code is stored in the src directory.  Test images are in the images directory.
